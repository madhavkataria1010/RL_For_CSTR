scenario:
  id: "uncertainty_pm10"
  tier: 1
  role:
    - "extended_benchmark"
  implementation_status: "scaffold_only"

benchmark_extended:
  perturbation_percent: 10
  parameters:
    - "ca_in"
    - "ua"
    - "k0_group_scale"
  schedule:
    mode: "one_at_a_time_and_aggregate"
    values_percent: [-10, 10]
  notes:
    - "Use the same perturbation schedule per seed across all compared methods."
